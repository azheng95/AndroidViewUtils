<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidUtils"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".imagepicker.ImagePickerDemoActivity"
            android:exported="false"
            android:launchMode="standard"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

        <!-- ==================== Edge-to-Edge Demo ==================== -->

        <!-- Edge Demo 列表入口 -->
        <activity
            android:name=".edgedemo.EdgeDemoListActivity"
            android:exported="false"
            android:label="Edge-to-Edge Demo" />

        <!-- 在 AndroidManifest.xml 的 <application> 标签内添加 -->

        <activity android:name=".edgedemo.BasicEdgeActivity" />
        <activity android:name=".edgedemo.DarkThemeEdgeActivity" />
        <activity android:name=".edgedemo.AutoThemeEdgeActivity" />
        <activity android:name=".edgedemo.ImmersiveEdgeActivity" />
        <activity android:name=".edgedemo.ImeEdgeActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".edgedemo.CustomConfigEdgeActivity" />
        <activity android:name=".edgedemo.ExtensionDemoActivity" />
        <activity android:name=".edgedemo.SystemBarsHelperActivity" />
        <activity android:name=".edgedemo.NavigationModeActivity" />
        <activity android:name=".edgedemo.BaseActivityDemoActivity" />

        <activity
            android:name=".sequentialanimator.SequentialAnimDemoActivity"
            android:label="Sequential Animation Demo"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false" />


    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidUtils"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ImagePickerDemoActivity"
            android:exported="false"
            android:launchMode="standard"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

        <!-- ==================== Edge-to-Edge Demo ==================== -->

        <!-- Edge Demo 列表入口 -->
        <activity
            android:name=".edgedemo.EdgeDemoListActivity"
            android:exported="false"
            android:label="Edge-to-Edge Demo" />

        <!-- 完整功能演示 -->
        <activity
            android:name=".edgedemo.DemoEdgeActivity"
            android:exported="false"
            android:label="完整功能演示" />

        <!-- 简单用法示例 -->
        <activity
            android:name=".edgedemo.SimpleDemoActivity"
            android:exported="false"
            android:label="简单用法示例" />

        <!-- 扩展函数用法示例 -->
        <activity
            android:name=".edgedemo.ExtensionDemoActivity"
            android:exported="false"
            android:label="扩展函数用法" />

        <!-- 沉浸式全屏示例 -->
        <activity
            android:name=".edgedemo.ImmersiveDemoActivity"
            android:exported="false"
            android:label="沉浸式全屏"
            android:theme="@style/Theme.AndroidUtils.NoActionBar" />

    </application>

</manifest>